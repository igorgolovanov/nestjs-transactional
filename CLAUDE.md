# @nestjs-transactional monorepo

The agent onboarding context for this repository lives in
[`AGENTS.md`](AGENTS.md). Read that file in full at the start of
every session — it carries the project overview, mission, tech
stack, ADR / DD indexes, the DO NOT cheat-sheet, quality gates,
session-onboarding instructions, and the current status with
pointers to the per-topic docs under [`docs/`](docs/).

This file exists as a pointer for tools that look for `CLAUDE.md`
specifically. The content is canonical in `AGENTS.md`.
