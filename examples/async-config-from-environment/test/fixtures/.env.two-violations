# Fixture: two independent rules broken at once. PG_HOST is omitted
# and OUTBOX_POLLING_INTERVAL_MS = 0 violates min(50). Both must
# appear in one error, which is what `abortEarly: false` buys.
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=postgres
PG_DATABASE=async_config_dev

OUTBOX_POLLING_INTERVAL_MS=0
OUTBOX_BATCH_SIZE=50
OUTBOX_MAX_CONCURRENT=10
