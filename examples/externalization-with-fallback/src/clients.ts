/**
 * One ClientProxy registration — RabbitMQ for the visual demo, mocked
 * in jest. The headline of this example is what a COMPLETED
 * publication proves and the consumer-side inbox, not multi-broker
 * routing.
 */
export const REFUNDS_BROKER = 'REFUNDS_BROKER';
